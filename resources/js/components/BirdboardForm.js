class BirdboardForm {
    constructor(data) {
        this.data = data;
        // form.title
        // form.description

        Object.assign(this, data);
    }

    submit(endpoint) {
        
    }
}

export default BirdboardForm;